    import XCTest
    @testable import Relector

    final class RelectorTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(Relector().text, "Hello, World!")
        }
    }
